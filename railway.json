{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @store/shared prisma:generate:ci && pnpm --filter shared build && pnpm --filter backend build:prod"
  },
  "deploy": {
    "startCommand": "node apps/backend/dist/main",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
